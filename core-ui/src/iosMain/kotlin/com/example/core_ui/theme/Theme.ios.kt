package com.example.core_ui.theme

import androidx.compose.runtime.Composable

@Composable
actual fun PlatformThemeSetup() {
    // iOS-specific theme setup if needed
    // For now, this is empty since iOS doesn't need special theme setup
}